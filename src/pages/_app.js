import "@/styles/globals.css";

export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />; //component is the page like index.js and pageprops is the data fetched for that page
}
